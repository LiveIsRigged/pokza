# macOS
.DS_Store

# Supabase CLI cache (local state, jamais versionné)
supabase/.temp/

# Identifiants seed de DEV — ne pas publier sur le repo public
docs/dev/seed-passwords.sql

# Sortie de compilation des scripts de test (regénérée par le tsc local, cf. l'en-tête des scripts)
scripts/b3/
scripts/ref/
scripts/precis/
scripts/n6000/
scripts/avant/
scripts/fe/
scripts/pe/
scripts/cm/
scripts/fc/
scripts/ch/
scripts/lv/
scripts/i18n/
scripts/i18n-*.csv
# Traductions en attente d'import (cf. scripts/i18n-remplir.js). Volontairement NON suivies, pour
# la même raison que les CSV : une fois importées, le catalogue EST le document de référence. En
# garder une seconde copie, c'est se donner deux vérités qui divergeront à la première correction.
scripts/traductions/
scripts/legal/

# Banc de traduction : résultats et journal des neurons dépensés (regénérés, cf. scripts/traduction-banc.mjs)
scripts/traduction/
